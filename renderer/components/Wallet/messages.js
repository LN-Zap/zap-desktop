import { defineMessages } from 'react-intl'

/* eslint-disable max-len */
export default defineMessages({
  pay: 'Pay',
  request: 'Receive',
  request_disabled_memo: 'You need to have on-chain balance first',
  menu_item_autopay: 'Autopay',
})
